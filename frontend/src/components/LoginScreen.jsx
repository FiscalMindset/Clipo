import AuthScreen from './AuthScreen';

// Google OAuth remains available in lib/auth.js and backend/routes/auth.py,
// but this screen intentionally uses email/password while it is disabled.
export default AuthScreen;
